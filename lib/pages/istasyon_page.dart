import 'package:flutter/material.dart';

class Istasyon extends StatelessWidget {
  const Istasyon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('İstasyonlarımız'),
    );
  }
}
